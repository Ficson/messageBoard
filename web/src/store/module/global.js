import Vue from 'vue'
let _this = Vue.prototype

const state = {
  // orderMessage: {}, // 订单详情
}
const mutations = {
  // SET_ORDER_MESSAGE: (state, orderMessage) => {
  //   state.orderMessage = orderMessage
  // },

}
const actions = {

}

export default {
  state,
  mutations,
  actions
}
